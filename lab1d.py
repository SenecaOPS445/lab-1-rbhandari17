#!/usr/bin/env python3

# Define the integer objects
x = 10
y = 2
z = 5

# Perform the calculation (using the order of operations: PEMDAS)
result = x + y * z

# Print the result in the desired format
print(f"{x} + {y} * {z} = {result}")
